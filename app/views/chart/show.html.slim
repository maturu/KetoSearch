- set_meta_tags title: "#{@food.name}の情報", description: "#{@food.name}の糖質・タンパク質・脂質などの情報について。#{@food.content}",\
  keywords: "糖質検索, ケトジェニック, ダイエット, 野菜 糖質, 糖質 一覧 100g, 糖質制限, ケトサーチ, ketosearch, ketogenic, diet, ローカーボ, ロカボ, #{@food.name} 糖質, ケトサーチ #{@food.name}"

div.p-show
  div.s-chart
    div.s-chart-content
      div.s-chart-wrap
        div.flex-100
          div.s-chart-widget-container
            div.chart-header-title#top-header
              div.title-content
                h1
                  = @food.name
              = render 'chart/menu', food: @food
            div.chart-info
              div.info-item
                span.mr-1
                  = link_to @user.username, users_show_path(username: @user.username), class: "mr-1 underline"
                  |(投稿者), 
                span
                  = link_to "##{@food.tag}", ranking_show_path(tag: @food.tag), class: "underline"
                span.icon-text-separator |
              div.info-item
                - unless @food.address.blank?
                  span.mr-1
                    = link_to "#{@food.address}", "https://maps.google.co.jp/maps/search/#{@food.address}",\
                    target: "_blank", rel: "noopener noreferrer", class: "mr-1 underline"
                    |(住所),
              div.info-item
                - unless @food.reference.blank? and @food.url.blank?
                  span.mr-1
                    = link_to @food.reference, @food.url, class: "mr-1 underline", target: "_blank", rel: "noopener noreferrer"
                    |(文献)

        div.flex-50
          div.s-chart-widget-container
            div.chart-header-title
              div.title-content
                h2
                  i.fas.fa-receipt.mr-1
                  |「
                  = @food.name
                  |」の主要成分
                  span.chart-unit-text
                    |/ 
                    = @food.gram
                    |g あたり
            div.chart-table.position-relative
              table.table-content
                colgroup
                  col.table-header-col width="20%"
                  col width="8%"
                  col width="8%"
                  col width="8%"
                  col width="8%"
                  col width="8%"
                  col width="8%"
                  col width="8%"
                tbody
                  tr
                    th.table-header rowspan="2"
                      |一般栄養成分
                      br
                      span
                        |- - ： データがありません 
                    td.table-header-color
                      span カロリー
                    td.table-header-color
                      span 糖質
                    td.table-header-color
                      span タンパク質
                    td.table-header-color
                      span 脂質
                    td.table-header-color
                      span 食物繊維
                    td.table-header-color
                      span ナトリウム
                    td.table-header-color
                      span 水分
                  tr
                    td #{@food.calorie or "- -"}
                    td #{@food.carbohydrate or "- -"}
                    td #{@food.protain or "- -"}
                    td #{@food.lipid or "- -"}
                    td #{@food.fibtg or "- -"}
                    td #{@food.na or "- -"}
                    td #{@food.water or "- -"}
                  tr
                    th.table-header 単位
                    td kcal
                    td g
                    td g
                    td g
                    td g
                    td mg
                    td g

        div.flex-50
          div.s-chart-widget-container
            div.chart-header-title
              div.title-content
                h2
                  i.fas.fa-allergies.mr-1
                  = "「#{@food.name}」のアレルギー物質"
            div.chart-table.position-relative
              - if @food.enter_allergies?
                table.table-content
                  colgroup
                    col.table-header-col width="20%"
                    col width="8%"
                    col width="8%"
                    col width="8%"
                    col width="8%"
                    col width="8%"
                    col width="8%"
                    col width="8%"
                  tbody
                    tr
                      th.table-header rowspan="2"
                        |特定原材料7品目
                        br
                        span
                          |● ： 使用しています
                          br
                          |─ ： 使用していません
                      td.table-header-color
                        span 卵
                      td.table-header-color
                        span 乳
                      td.table-header-color
                        span 小麦
                      td.table-header-color
                        span 落花生
                      td.table-header-color
                        span えび
                      td.table-header-color
                        span かに
                      td.table-header-color
                        span そば
                    tr
                      td = @food.egg? ? "●" : "─"
                      td = @food.milk? ? "●" : "─"
                      td = @food.wheat? ? "●" : "─"
                      td = @food.peanut? ? "●" : "─"
                      td = @food.shrimp? ? "●" : "─"
                      td = @food.crab? ? "●" : "─"
                      td = @food.buckwheat? ? "●" : "─"
                    tr
                      th.table-header
                        |特定原材料に
                        br
                        |準ずるもの20品目
                      td colspan="7" = @food.allergies.blank? ? "─" : @food.allergies
              - else
                div.widget-error
                  i.fas.fa-exclamation-triangle
                  p ここに表示するデータはありません。

        div.flex-100
          div.s-chart-widget-container
            div.chart-header-title
              div.title-content
                h2
                  i.fas.fa-chart-pie.mr-1
                  |「
                  = @food.name
                  |」の成分割合
                  span.chart-unit-text
                    |/ 
                    = @food.gram
                    |g あたり
            div.chart-widget-wrapper
              div.widget-wrapper-content
                div.pie-chart
                  = pie_chart @f_info
              - unless @items.blank?
                div.widget-wrapper-content
                  div.relation-content
                    - @items.each_with_index do |item, index|
                      = link_to chart_show_path(id: item[0]), class: "relation-content-item"
                        div.relation-item-name
                          span.label-line-number.mr-2 = index+1
                          = item[1]
                        div.progress-value
                          = item[2]
                          |g
                        div.progress-bar-content
                          div.progress-bar style="width: #{item[2]}px;"
            div.chart-paginate-content
              div.paginate-empty
              = paginate @relations

        div.flex-100
          div.s-chart-widget-container
            div.chart-header-title
              div.title-content
                h2
                  i.fas.fa-file-alt.mr-2
                  |詳細
            div.preview-content
              - if @food.content?
                div.md-text
                  - @food.content.split("\n").each do |text|
                    = markdown(text).html_safe
              - else
                div.widget-error
                  i.fas.fa-exclamation-triangle
                  p ここに表示するデータはありません。
